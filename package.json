{
  "name": "battle-brawlers",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "npx vercel env pull .env.local",
    "dev": "../dev.sh 'bunx vite --port $PORT'",
    "build": "bunx vite build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "git config core.hooksPath .githooks"
  },
  "dependencies": {
    "@ape-egg/vibe": "^1.9.1",
    "async-await-websockets": "^3.0.5",
    "howler": "^2.2.4",
    "seedrandom": "^3.0.5"
  },
  "devDependencies": {
    "@ape-egg/vite-plugin-vibe": "^0.0.2",
    "prettier": "^3.8.3",
    "vite": "^8.0.8"
  }
}
